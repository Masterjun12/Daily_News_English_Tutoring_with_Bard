project/
│
├── main.py                   # Streamlit 애플리케이션의 실행 파일
├── news_crawler.py           # 뉴스 크롤링을 담당하는 파일
├── translator.py             # 번역을 담당하는 파일
├── ai_quiz_generator.py   # 문제 생성을 담당하는 파일
├── utils.py                  # 공통 함수들 (선택적)
└── config.py                 # 설정 파일 (선택적)
